# Pangolin Proxy

Small reverse proxy / tunneling setup based on **Pangolin**, intended for simple self-hosted services and microVM deployments.

This repo uses `mise` for tool/version management.

---

## Requirements

- Pangolin
- `mise` (https://mise.jdx.dev)

Tool versions are defined in `mise.toml`.

---

## Setup

```bash
mise install
